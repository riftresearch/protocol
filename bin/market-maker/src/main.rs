use alloy_primitives::Address;
use eyre::Result;
use log::info;
use std::str::FromStr;
use std::sync::Arc;

use auction_claimer::{AuctionClaimer, AuctionClaimerConfig};

use crate::auction_claimer;

#[tokio::main]
async fn main() -> Result<()> {
    Ok(())
}
